apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: authorizations-rotate-keys-role-bind
  namespace: ${NAMESPACE}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: authorizations-rotate-keys-role
subjects:
  - kind: ServiceAccount
    name: authorizations-rotate-keys-service-account
    namespace: ${NAMESPACE}
