apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: gateway-rotate-keys-role
  namespace: default
rules:
  - apiGroups: ["apps", "extensions"]
    resources: ["deployments"]
    resourceNames: ["monsters-gateway"]
    verbs: ["get"]
