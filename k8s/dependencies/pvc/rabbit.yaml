apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rabbit-pvc
  namespace: ${NAMESPACE}
spec:
  storageClassName: "" 
  volumeName: rabbit-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: ${VOLUME_SIZE_RABBIT}Gi
