apiVersion: v1
kind: Service
metadata:
  labels:
    app: graphana
  name: graphana
  namespace: ${NAMESPACE}
spec:
  selector:
    app: graphana
  ports:
    - name: gui
      port: 3000
      targetPort: 3000
